"""
Child Agent Runtime — offline-first agent with local identity, policy engine,
RAG, encrypted storage, and signed audit logging.

Entry point: python -m child_agent_runtime {register|sync|run-offline|upload-audit}
"""

__version__ = "0.1.0"
